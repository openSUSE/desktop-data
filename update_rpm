#! /bin/sh

opwd=$PWD
tdir=`mktemp -d`
cd $tdir || exit 1
svn export http://svn.suse.de/svn/desktop/branches/SUSE_10_1_BRANCH/desktop-data
rm desktop-data/update_rpm
tar -cvj -f $opwd/desktop-data.tar.bz2 desktop-data
cd /
rm -rf $tdir
